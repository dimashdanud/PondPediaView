<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginBottom="1dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="5dp"
    app:cardPreventCornerOverlap="true"
    app:cardUseCompatPadding="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="20dp"
        android:gravity="center">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/pond_image"
            android:layout_width="110dp"
            android:layout_height="110dp"
            android:layout_marginHorizontal="1dp"
            android:layout_marginTop="5dp"
            android:src="@drawable/ic_baseline_games_24"
            android:contentDescription="@string/pond_name"
            android:visibility="visible"
            app:shapeAppearanceOverlay="@style/circleImageView"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
        <TextView
            android:id="@+id/pond_name"
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:layout_below="@id/pond_image"
            android:layout_alignStart="@id/pond_image"
            android:layout_alignEnd="@id/pond_image"
            android:gravity="center"
            android:text="@string/pond_name"
            android:textStyle="bold"
            android:textSize="15sp"/>

        <TextView
            android:id="@+id/pond_fish"
            android:layout_width="90dp"
            android:layout_height="wrap_content"
            android:layout_below="@id/pond_name"
            android:layout_alignStart="@id/pond_name"
            android:layout_alignEnd="@id/pond_name"
            android:gravity="center"
            android:text="@string/pond_fish"
            android:textStyle="italic"
            android:textSize="11sp"/>
    </RelativeLayout>

</androidx.cardview.widget.CardView>